DATA_GO_KR_SERVICE_KEY=
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/prop_trend
# 갱신 API(/jobs/refresh) 인증키. openssl rand -hex 32 등으로 만들어 채운다.
# 비워두면 갱신 API가 503으로 잠긴다(예약 실행은 그대로 돈다).
REFRESH_API_KEY=
